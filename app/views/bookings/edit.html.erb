<div id="wrap-content">
<div class="rapp-form">
  <%= simple_form_for [@event, @booking] do |f| %>
    <%= f.select :status , [['Accepted'],['Declined']] %>

    <%= f.input :answer%>
    <%= f.submit %>
  <% end %>
</div>
</div>

